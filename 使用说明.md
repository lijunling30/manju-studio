# 使用说明 · 漫镜工场 ManJu Studio

本文档是平台的操作手册，面向**使用者**。内容涵盖：启动与停止、9 步流水线的逐步操作、确认闸口与成本设置、以及常见问题排查。

部署层面的内容（环境安装、配置项含义）见 [DEPLOY.md](DEPLOY.md)；架构与设计原理见 [项目报告.md](项目报告.md)。

---

## 目录

- [一、启动与停止平台](#一启动与停止平台)
- [二、两种运行模式](#二两种运行模式)
- [三、从零到成片：9 步操作详解](#三从零到成片9-步操作详解)
- [四、确认闸口怎么用](#四确认闸口怎么用)
- [五、模型服务设置](#五模型服务设置)
- [六、成本中心与预算护栏](#六成本中心与预算护栏)
- [七、任务中心](#七任务中心)
- [八、常见问题](#八常见问题)
- [九、数据与文件位置](#九数据与文件位置)

---

## 一、启动与停止平台

### 首次使用

确保已安装 Python 3.10+ 与 Node.js 18+，然后双击 **`deploy.bat`**。脚本会自动完成环境检查、依赖安装、配置生成、建库与启动，最后自动打开浏览器。

### 日常使用

| 操作 | 方式 |
|---|---|
| 启动 | 双击 `start_manju.bat`，等待进度框提示「服务已就绪」 |
| 访问 | 浏览器打开 <http://localhost:3000> |
| 停止 | 关闭启动器提示框，或执行 `powershell -ExecutionPolicy Bypass -File start_manju.ps1 -Command stop` |
| 查看状态 | `powershell -ExecutionPolicy Bypass -File start_manju.ps1 -Command check` |

> 启动器带有**空闲守护**：平台长时间无操作且没有打开工作台页面时会自动停止服务、释放内存。下次双击 `start_manju.bat` 即可恢复。

### 重置数据

关闭服务后，删除 `backend/manju.db` 文件，重新启动后端即可得到一个全新空库。已生成的图片/视频等资产位于 `backend/storage/`，如需彻底清理请一并删除。

---

## 二、两种运行模式

模式由 `backend/.env` 中的 `MOCK_MODE` 控制，改完需要**重启后端**才生效。

| 模式 | 配置 | 表现 | 适用场景 |
|---|---|---|---|
| 模拟模式 | `MOCK_MODE=true` | 全部 AI 调用走内置 Mock 厂商，图片为占位图、视频为占位片段，1~8 秒返回 | 零成本体验完整流程、演示、开发调试 |
| 真实模式 | `MOCK_MODE=false` + 填 Key | 调用 DeepSeek / 通义万相 / HappyHorse / Qwen-Audio，产出真实内容 | 实际生产 |

> 真实模式下**每一次调用都会真实计费**。建议先保持模拟模式把流程走通、熟悉确认闸口，再切换到真实模式。

---

## 三、从零到成片：9 步操作详解

登录后进入仪表盘，可以看到 9 步流程入口；左侧边栏的「制作流程」也提供了同样的导航，并且会标出每一步的完成状态。

### 第 0 步 · 注册与登录

打开 <http://localhost:3000> 会进入登录页。首次使用请先注册账号，注册后自动登录。

- 登录态基于 JWT，保存在浏览器本地；
- 多用户之间数据相互隔离，各自拥有独立的项目、角色资产库与账单。

### 第 1 步 · 新建项目

在左侧边栏「项目」区域点「新建项目」，填写项目名称等基础信息。

- 项目是整个流水线的容器：小说、剧本、角色、分镜、成片、成本都归属于项目；
- 侧边栏可对项目进行**编辑**与**删除**（删除按钮需点击两次确认，防误删）。

### 第 2 步 · 小说生成

进入「小说生成」页面，填写**题材**与**主角人设**等要素，点击生成。

- 提交后会弹出**确认卡**（详见第四节），确认后任务进入后台 Worker；
- 完成后可在页面上查看小说大纲与章节内容，并可继续编辑修改；
- 生成的章节数、每章字数由 `backend/.env` 中的 `NOVEL_CHAPTER_WORDS` 等参数控制。

### 第 3 步 · 剧本结构化

进入「剧本结构化」页面，将小说转换为结构化剧本：场景拆分、对白、情绪曲线等。

- 剧本编辑器支持逐场修改，情绪曲线会随内容变化；
- 结构化的结果会直接决定后续分镜的拆分粒度，建议这一步多花些时间打磨。

### 第 4 步 · 角色资产库

角色资产库用于保证**同一角色跨镜头不变脸**，是整个平台一致性的基础。

1. 平台会依据小说/剧本内容自动生成角色清单；
2. 每个角色先生成**三视图**（正面 / 侧面 / 背面）的多个候选，附带 AI 评分，由你挑选一张确认；
3. 确认三视图后，可继续生成**表情集**候选并挑选；
4. 角色资产按项目组织成子库，可跨项目复用——同名角色在后续项目中会被自动复用，跳过重复生成。

> **一致性原理**：所有角色相关生成都会引用同一份角色特征描述块，表情集与选中的三视图使用相同的随机种子，从而最大程度保持外观一致。

### 第 5 步 · 分镜设计

进入「分镜时间线」，把剧本拆解为一个个镜头。

- 每个镜头包含画面描述、时长、出场角色、关联的参考图等信息；
- 可在时间线上增删镜头、调整顺序、编辑单镜头的描述；
- 镜头里勾选的出场角色会被带入后续生成的提示词中，用于一致性约束。

### 第 6 步 · 关键帧抽卡

进入「关键帧抽卡」页面，为镜头批量生成候选画面。

- 每个镜头一次生成多个候选关键帧，并附 AI 评分，由你挑选最满意的一张作为该镜头的定稿；
- 抽卡是**批量任务**，会受确认闸口的批量护栏约束（≥20 镜头强制确认）；
- 定稿关键帧将作为下一步图生视频的输入。

### 第 7 步 · 视频生成

进入「视频生成」页面，基于已确认的关键帧生成镜头视频。

- 视频生成是**长时异步任务**（单镜头可能数分钟），提交后可在任务中心查看进度，无需守在页面上；
- 主厂商调用失败时，平台会自动切换到备选厂商重试；连续失败 3 次会转人工处理；
- 页面会聚合展示各厂商、各镜头的生成状态与结果。

### 第 8 步 · 配音与音效

进入「配音音效」页面，为画面配上语音与音乐。

- 台词配音使用 TTS 模型，音色可在设置页统一指定；
- BGM 与音效目前为占位资源（套餐内未开通对应模型），不影响流程完整性。

### 第 9 步 · 剪辑合成与导出

进入「剪辑合成导出」页面，把镜头、配音、音效合成为成片。

- 合成由服务端完成，页面展示渲染进度；
- **导出成片会强制携带 AI 生成标识**（无开关可关闭），这是合规底线；
- 若开启了「合规检验」模块，导出前会先执行内容安全审核，审核结果会记录下来。

---

## 四、确认闸口怎么用

确认闸口是平台的成本安全机制：**任何一次 AI 生成请求，都会先复述需求并预估成本，你确认之后才真正调用付费接口。**

### 确认卡

发起任意 AI 操作后，屏幕中央会浮出确认卡，包含四块信息：

| 区域 | 内容 |
|---|---|
| 意图复述 | AI 把你这次要做什么用结构化语言复述一遍 |
| 参数摘要 | 本次调用的关键参数（数量、时长、模块等） |
| 预估成本 | 本次调用的成本区间（下限 ~ 上限） |
| 操作 | **确认执行** / **修改** / **放弃** |

- 点「**修改**」可输入修改意见，AI 会重新复述需求，最多 3 轮，超过则转人工；
- 点「**放弃**」立即取消，**不产生任何费用**；
- 若「确认执行」时执行失败，卡片**不会自动关闭**，而是就地显示错误，方便你重试或放弃；
- 确认卡上有「**本次会话不再确认**」开关，仅对当前浏览器会话有效，**刷新页面即恢复安全态**。

### 三级开关（设置页）

进入「设置」页面可调整闸口开关，分为三级：

| 层级 | 生效范围 | 持久性 |
|---|---|---|
| 全局开关 | 所有模块 | 持久保存 |
| 模块开关 | 指定模块（小说生成 / 剧本结构化 / 分镜设计 / 角色形象 / 关键帧抽卡 / 视频生成 / 配音音效 / 剪辑合成 / 合规检验） | 持久保存 |
| 会话开关 | 当前浏览器会话 | 刷新即失效 |

### 高成本护栏

即使把上面三级开关**全部关闭**，以下两种情况仍然会强制弹出确认卡：

- 单次预估成本 **≥ 50 元**；
- 批量任务 **≥ 20 个镜头**。

阈值可在 `backend/.env` 中通过 `GATE_HIGH_COST_THRESHOLD` 与 `GATE_BATCH_THRESHOLD` 调整。

### 超时

确认卡默认 **60 秒**无操作会自动超时取消，同样**不产生费用**。超时时间由 `GATE_TIMEOUT_SECONDS` 控制。

---

## 五、模型服务设置

进入「设置 → 模型服务」，可以为每一类任务指定具体调用的 AI 模型：

| 配置项 | 作用范围 |
|---|---|
| 文本模型 | 小说 / 剧本 / 分镜 |
| 图像模型 | 角色三视图 / 表情 / 关键帧 |
| 视频模型 | 图生视频 / 参考生视频 |
| TTS 模型 | 语音合成 |
| TTS 音色 | 配音音色 |

- 每一项都提供「默认」选项，**留空即使用系统默认模型**（即 `.env` 中的配置）；
- 修改后点「保存」，偏好按用户存储，下次登录依然生效；
- 使用建议：日常批量生产用较便宜的模型（如 `deepseek-v4-flash`），关键镜头再切到更强的模型重做。

---

## 六、成本中心与预算护栏

进入「成本中心」页面，可以查看和控制在 AI 调用上的花销。

### 页面内容

| 区块 | 说明 |
|---|---|
| 指标卡 | 已消耗、预算上限、使用率、单分钟成本 |
| 按模块消耗 | 小说 / 剧本 / 分镜 / 角色 / 关键帧 / 视频 / 配音 / BGM / 音效 / 剪辑 / 合规 各模块的花费拆分 |
| 用户账单 | 按厂商汇总的调用次数与金额 |
| 成本流水 | 镜头级记账明细（时间 / 模块 / 厂商 / 镜头 / 金额） |

### 设置预算

在页面顶部选择项目并填写「项目预算（元）」，点保存即可生效。

预算护栏分两档：

- 使用率达到 **80%**：页面提示「预算已达 80%，注意控制」；
- 使用率达到 **100%**：提示「预算已用尽，AI 生成将被拦截」，此后该项目的计费任务会被直接拒绝创建。

> 成本控制是**双保险**：生成前由确认闸口拦截（预估成本），记账前由预算护栏拦截（实际花费）。

---

## 七、任务中心

任务中心悬浮在页面顶部，滚动页面时会收起为一枚小胶囊。

- 徽章显示「N 个任务进行中」与「N 个任务需处理」；
- 点开后是完整任务列表，每条显示模块类型、状态、进度，以及「重试 x/y」次数；
- 任务状态含义：

| 状态 | 含义 |
|---|---|
| queued | 已排队，等待 Worker 派发 |
| running | 执行中 |
| success | 已完成 |
| failed | 本次失败 |
| retrying | 失败后按 2s / 4s / 8s 退避重试中 |
| manual_review | 重试 3 次仍失败，或运行超时卡死，需人工介入 |
| cancelled | 已被取消 |

> 出现 `manual_review` 时，请先到「设置 → 模型服务」确认 Key 与模型配置是否正确，再手动重试。

---

## 八、常见问题

| 问题 | 解决办法 |
|---|---|
| `deploy.bat` 双击闪退 | 右键「以管理员身份运行」；或先执行 `Set-ExecutionPolicy Bypass -Scope CurrentUser` |
| `npm install` / `pip install` 很慢 | 换国内镜像：`npm config set registry https://registry.npmmirror.com`；pip 换清华源 |
| 端口 3000 / 8000 被占用 | 执行 `powershell -File start_manju.ps1 -Command stop` 清理本平台进程 |
| 图像生成频繁失败、报 429 | 套餐 Key 有 QPS 限制，请在 `.env` 配置 `DASHSCOPE_PAY_API_KEY`（按量 Key 不限流） |
| 图生视频失败 | 确认图像/视频走的是按量 Key；平台会自动复用万相返回的公网 URL，无需自行做公网穿透 |
| 生成出来是占位图 / 占位视频 | 当前处于模拟模式，把 `.env` 中 `MOCK_MODE` 改为 `false` 并重启后端 |
| 任务长时间停在 `manual_review` | 检查 API Key、余额与模型名是否正确，然后在任务中心手动重试 |
| 想换成 PostgreSQL | `.env` 改 `DATABASE_URL=postgresql+psycopg://...`，并 `pip install psycopg-binary` 后重启 |
| 忘记密码 / 想重置账号 | 目前无找回入口，停服删除 `backend/manju.db` 后重新注册 |

---

## 九、数据与文件位置

| 路径 | 内容 |
|---|---|
| `backend/manju.db` | SQLite 数据库（用户、项目、剧本、镜头、成本等全部业务数据） |
| `backend/storage/` | 生成资产：关键帧图片、镜头视频、配音音频等 |
| `backend/.env` | 运行配置（模式开关、API Key、闸口参数、数据库地址） |
| `logs/` | 启动 / 停止 / 后端 / 前端 / 守护进程日志 |
| `平台截图/` | 界面截图，可用于文档与展示 |

> `backend/.env` 与 `backend/storage/` 均已被 `.gitignore` 排除，不会被提交到版本库。请不要把包含 API Key 的配置文件分享给他人。